---DESCRIPTION---
Must use spaces use tabs
---CONTENTS---
<?php

function (array $foo) : iterable {
	foreach ($foo as $bar) {
		yield $bar;
	}
}

---FIXED---
<?php

function (array $foo) : iterable {
    foreach ($foo as $bar) {
        yield $bar;
    }
}

---
